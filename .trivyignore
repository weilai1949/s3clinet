# Trivy 漏洞忽略清单：仅在 CVE 没有可升级的修复版本、或修复会破坏功能/兼容性时使用。
# 每条忽略都需有理由 + 跟踪 ID；优先用以下方式消化漏洞（按优先级）：
#   1. 升级 base 镜像到当前 stable（alpine 团队会很快 backport 修复）
#   2. 重建镜像（部分 CVE 在重新构建后随新发布的包消失）
#   3. 仅当以上无法解决时，使用本文件忽略
#
# 格式：CVE-YYYY-NNNN  +  简述 + 跟踪 issue/备注
#
# 当前无主动忽略项；alpine:3.20 体积小、依赖少，理论上 CRITICAL/HIGH 应极少。
# 一旦出现，先在 https://cve.mitre.org 确认利用条件与暴露面，再决定忽略或升级 base。
